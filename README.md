# Merged Blocklists

This project automatically merges multiple DNS blocklists into a single deduplicated list.

## Files
- data/blocklist-merged.txt — final merged list
- scripts/merge.sh — script that generates the list

## Purpose
To maintain a clean, unified blocklist for DNS filtering.
